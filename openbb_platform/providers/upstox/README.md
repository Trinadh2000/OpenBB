# OpenBB Upstox Provider

This extension integrates the [Upstox](https://upstox.com/) data provider into the OpenBB Platform.

## Installation

```bash
pip install openbb-upstox
```

## Authorization

This extension requires an `access_token` obtained from Upstox.
Add it under `credentials` in `~/.openbb_platform/user_settings.json`.
